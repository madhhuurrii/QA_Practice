package code.string;

public class string_method {
  public static void main(String[] args) {
      // String methods
      // s.length() - returns length of the string
      // concat() - joining 2 or more strings
      // trim() - remove spaces from right and left of the string
      // charAt() - returns character from string based on index (starts from 0)
      // contains() - checks whether it has the character/string in main string (case sensitive)
      // equals() - Compares two string (case sensitive)
      // equalsIgnoreCase() - Compares two string (ignores case)
      // replace('e','x') - replaces single/multiple(sequence) of characters in string
      // substring(firstindex, lastindex) - extracts substring from main string
      // toUpperCase(), toLowerCase()


      
  }
}
